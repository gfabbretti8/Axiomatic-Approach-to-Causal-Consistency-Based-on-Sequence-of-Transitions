
\begin{definition}[Reverse Function]
  Given a sequence $\sigma$ we define $\rev{\sigma}$ as the
  following $rev$ function:

  \[
    rev(\alpha\sigma) = rev(\sigma)\rev{\alpha}
  \]
  \[
    rev(\es) = \es
  \]
  
\end{definition}

